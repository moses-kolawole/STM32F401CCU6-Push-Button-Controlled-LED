# STM32F401CCU6-Push-Button-Controlled-LED
This project demonstrates how to configure a GPIO pin as an input and read its state using the Input Data Register (IDR). A push button is connected to PA0, while an external LED is connected to PA6.
